import { LongPressActionDirective } from './long-press-action.directive';

describe('LongPressActionDirective', () => {
  it('should create an instance', () => {
    const directive = new LongPressActionDirective();
    expect(directive).toBeTruthy();
  });
});
